Directory structure:

    signal-BLOCKING: Check if SIGINT is blocked in process or not then unblock/block SIGINT
    signal-SIGINT: Write a program to print any message when pressing the key combination Ctrl+C.
                   Register actions for SIGUSR1 and SIGUSR2.

In each folder, perform below instructions:

    make: build excutable file.
    make clean: clean everything.