Directory structure:

    named_pipe: Create FIFO, exchange data between unrelated processes.
    related_pipe: Create Pipe, exchange data between related processes.

In each folder, perform below instructions:

    make: build excutable file.
    make clean: clean everything.